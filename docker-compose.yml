services:
  app:
    image: noeldemartin/proxy
    security_opt:
      - no-new-privileges:true
    volumes:
      - ./storage:/app/storage
      - ./.env:/app/.env
    restart: unless-stopped
    networks:
      default:
        aliases:
          - proxy

networks:
  default:
    name: nginx-agora-proxy
    external: true
